'use strict';

describe('myApp.colors module', function() {

  beforeEach(module('myApp.colors'));

  describe('colors controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ColorsCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});